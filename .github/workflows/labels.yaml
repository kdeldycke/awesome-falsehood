---
name: Labels
"on":
  push:
    branches:
      - main

jobs:

  labels:
    uses: kdeldycke/workflows/.github/workflows/labels.yaml@v0.9.1
    with:
      extra-label-files: |
        .github/labels-extra.json